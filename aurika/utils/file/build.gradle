plugins {
    id("java-library")
    id("maven-publish")
}

dependencies {
    api(project(":aurika:checker"))
    api(project(":aurika:utils:array"))
    api(project(":aurika:utils:string"))

    api(libs.iocommons)

    compileOnly(libs.annotations.jetbrains)
    compileOnly(libs.annotations.checker)
}

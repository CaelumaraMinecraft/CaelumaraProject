plugins {
    id("java-library")
    id("maven-publish")
}

group = "net.aurika.utils.checker"
version = project(":aurika").version

dependencies {
    implementation(api(project(":aurika:annotations")))
}

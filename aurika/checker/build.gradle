plugins {
    id("java-library")
    id("maven-publish")
}

dependencies {
    api(project(":aurika:annotations"))
    api(project(":aurika:util:function"))

    compileOnly(libs.annotations.jetbrains)
}

plugins {
    id("java-library")
    id("maven-publish")
}

dependencies {
    subprojects.forEach {
        api(it)
    }

    api(project(":dyanasis"))
    api(project(":aurika:config-api"))
    api(project(":validate"))

    api(libs.annotations.jetbrains)
    api(libs.annotations.checker)
}

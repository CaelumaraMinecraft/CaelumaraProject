plugins {
    id("java-library")
    id("maven-publish")
}

dependencies {
    api(project(":aurika:checker"))
    api(project(":aurika:utils"))
    api(libs.annotations.jetbrains)
    api(libs.annotations.checker)
    api(libs.caffeine)
    api(libs.adventure.api)
}

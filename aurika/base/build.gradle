plugins {
    id("java-library")
    id("maven-publish")
}

dependencies {
    api(project(":aurika:checker"))
    api(project(":aurika:util"))

    api(libs.caffeine)
    api(libs.adventure.api)

    compileOnly(libs.annotations.jetbrains)
    compileOnly(libs.annotations.checker)
}

plugins {
    id("java-library")
    id("maven-publish")
}

dependencies {
    api(project(":aurika:checker"))
    api(project(":aurika:utils:function"))
    api(project(":aurika:utils:string"))
    api(libs.annotations.jetbrains)
    api(libs.annotations.checker)
}

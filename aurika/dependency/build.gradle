plugins {
    id("java-library")
    id("maven-publish")
}

group = "net.aurika.data"
version = project(":aurika").version

dependencies {
    implementation(api(project(":aurika:base")))
    implementation(libs.asm.asm)
    implementation(libs.asm.commons)
}

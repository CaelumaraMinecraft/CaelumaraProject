plugins {
    id("java-library")
    alias(libs.plugins.kotlin)
    alias(libs.plugins.shadow)
}

group = "top.auspice.gradle"

dependencies {
    compileOnly(gradleApi())

    compileOnly(project(":base"))

    compileOnly(fileTree(".libs/"))

}

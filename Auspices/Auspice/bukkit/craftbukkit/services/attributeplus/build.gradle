plugins {
    id("java")
    alias(libs.plugins.kotlin)
    alias(libs.plugins.shadow)
}

group = "top.auspice.bukkit.services"

dependencies {
    compileOnly(project(":core:bukkit:craftbukkit:services:base"))

    compileOnly(fileTree("$rootDir/libs/services/attributeplus"))

    compileOnly(libs.server.craftbukkit)
}
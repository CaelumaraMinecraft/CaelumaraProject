plugins {
    id("java-library")
    alias(libs.plugins.kotlin)
    alias(libs.plugins.shadow)
}

group = "top.auspice.bukkit.services.base"

dependencies {
    implementation(api(project(":Auspice:services:base")))

    // 仅使用 Player.getUniqueId() 之类所有 Bukkit 下游分支服务端都有的 API
    compileOnly(libs.server.craftbukkit)
}

// 这些 Service 适用于所有 Bukkit 下游分支服务器

plugins {
    id("java-library")
    alias(libs.plugins.kotlin)
    alias(libs.plugins.shadow)
}

group = "top.auspice.paper.services"

dependencies {

    // 向外传递 API
    implementation(api(project(":core:bukkit:paper:services:base")))

    implementation(api(project(":core:bukkit:paper:services:serviceio")))

    implementation(api(project(":core:bukkit:spigot:services")))  // Spigot 服务器的插件同样适用于 Paper 服务器
//    implementation(api(project(":core:bukkit:craftbukkit:services")))  // CraftBukkit 服务器的插件同样适用于 Paper 服务器

}
plugins {
    id("java-library")
    alias(libs.plugins.kotlin)
    alias(libs.plugins.shadow)
}

group = "top.auspice.craftbukkit.services"

dependencies {
    // 向上传递依赖项
    implementation(api(project(":Auspice:bukkit:services:base")))

    implementation(api(project(":Auspice:bukkit:common:services:vault")))
    implementation(api(project(":Auspice:bukkit:common:services:skinsrestorer")))

    // 向下传递依赖项
    api(project(":Auspice:bukkit:services"))
//    api(libs.bukkit.craftbukkit)

    // ClassPath Libs
    compileOnly(project(":base"))
    compileOnly(libs.server.spigot)
}
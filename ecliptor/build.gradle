plugins {
    id("java-library")
    id("maven-publish")
}

version = "0.0.1-pre"
description = "一个多数据库类型的数据存取 API"

subprojects {
    it.version = this.version
}

dependencies {
    subprojects.forEach {
        this.dependencies.api(it)
    }
}

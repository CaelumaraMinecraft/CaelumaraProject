plugins {
    id("java")
    alias(libs.plugins.kotlin)
    alias(libs.plugins.shadow)
}

group = "top.mckingdom.powerful_territory"
version = "1.0.2"

dependencies {
    implementation(project(":v1.17:Auspice"))
}


var targetJavaVersion = 17
kotlin {
    jvmToolchain(targetJavaVersion)
}

tasks {

    build {
        dependsOn("shadowJar")
    }

    shadowJar {

        archiveBaseName = "Kingdoms-Addon-Powerful-Territory"
        archiveClassifier = null

        relocate("kotlin", "org.kingdoms.libs.kotlin")

        include()

//        exclude(
//            "kotlin-stdlib-2.0.20-Beta2.jar",
//            "annotations-13.0.jar",
//            "checker-qual-3.46.0.jar"
//        )
    }

    jar {
        enabled = false
    }


    processResources {
        Map<String, Object> props = new HashMap<>()
        props.put("version", this.version)
        inputs.properties(props)
        filteringCharset = "UTF-8"
        filesMatching("plugin.yml") {
            expand(props)
        }
    }
}

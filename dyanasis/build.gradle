plugins {
  id("java-library")
  alias(libs.plugins.kotlin)
}

description = "A dynamic, based on string placeholder system"

dependencies {
  api(project(":common:validate"))
  compileOnlyApi(libs.annotations.jetbrains)
  compileOnlyApi(libs.annotations.checker)
}

plugins {
    id("java-library")
//    alias(libs.plugins.kotlin)
//    alias(libs.plugins.shadow)
}

description = "一个占位符 API, 上下文系统"

dependencies {
    api(project(":common:validate"))
    compileOnly(libs.annotations.checker)
}

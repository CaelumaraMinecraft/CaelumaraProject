plugins {
  id("net.aurika.gradle.repository")
}

version
description = "A bukkit plugin like slimefun"

subprojects {
  repositories {
    mavenCentral()
    mavenLocal()
    aurikaRepos {
      spigotMC.snapshots()
      spigotMC.releases()
      paperMC.maven_public()
    }
  }
}

plugins {
  id("net.aurika.gradle.kingdoms")
}

version = "0.0.1-SNAPSHOT"
description = ""

dependencies {
  compileOnly(project(":auspice"))
}

kingdoms {
  addon {
    isAddonInterface = true
    addonName = "Territories"
  }
}

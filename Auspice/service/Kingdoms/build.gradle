plugins {
  id("net.aurika.gradle.kingdoms")
  alias(libs.plugins.shadow)
}

dependencies {
  compileOnly(project(":Auspice:service-api:bukkit-api"))
  compileOnly(fileTree("$rootDir/libs/plugins/Kingdoms_relocated"))
}

tasks {
  shadowJar {
    kingdomsRelocates.kingdomsRelocates()
  }
}

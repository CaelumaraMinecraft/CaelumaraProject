import net.aurika.gradle.AurikaExtReposKt

plugins {
    id("aurika.repos")
    id("java-library")
}

repositories {
    AurikaExtReposKt.essentials_releases(it)
    AurikaExtReposKt.essentials_snapshots(it)
}

dependencies {
    compileOnly(project(":Auspice:service-api:bukkit-api"))
    compileOnly("net.essentialsx:EssentialsX:2.20.1")
}

plugins {
    id("java-library")
    alias(libs.plugins.kotlin)
    alias(libs.plugins.shadow)
}

group = "net.aurika.auspice.bukkitlegacy.services"

dependencies {
    compileOnly(project(":base"))

    compileOnly(libs.server.legacy.craftbukkit)
}
plugins {
    id("java-library")
    alias(libs.plugins.forgegradle)
    alias(libs.plugins.kotlin)
    alias(libs.plugins.shadow)
}

group = "net.aurika.auspice.forge"

repositories {
}

dependencies {
    compileOnlyApi(libs.annotations.jetbrains)
}

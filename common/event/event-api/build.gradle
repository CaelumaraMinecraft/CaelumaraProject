plugins {
  id("java-library")
  id("maven-publish")
}

dependencies {
  api("net.aurika:common-key")                // keyed
  api("net.aurika:common-util-empty")         // empty arrays
  api(project(":validate"))                   // validate arguments
  compileOnlyApi(libs.annotations.jetbrains)  // annotations
}

java {
  sourceCompatibility = JavaVersion.VERSION_1_8
  targetCompatibility = JavaVersion.VERSION_1_8
}

plugins {
  id("java-library")
  id("maven-publish")
}

dependencies {
  api(project(":util:functional"))
  api(project(":validate"))
  api(libs.kyori.advntr.nbt)
  compileOnlyApi(libs.annotations.jetbrains)
}

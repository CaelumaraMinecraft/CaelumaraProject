plugins {
  id("java-library")
  id("maven-publish")
}

dependencies {
  api(project(":key"))
  api(project(":util:collection"))
  api(project(":util:stacktrace"))
  api(project(":validate"))
  compileOnlyApi(libs.annotations.jetbrains)
}

java {
  sourceCompatibility = JavaVersion.VERSION_11
  targetCompatibility = JavaVersion.VERSION_11
}
